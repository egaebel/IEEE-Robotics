#ifndef COLOR_SENSOR_H_
#define COLOR_SENSOR_H_

#include "pins.h"

/*
 * This is an empty useless class used in place of the actual
 * ColorSensor class until we can get it up and running
 */
class ColorSensor {
	
	public:
		COLOR detectColor() {

			return RED;
		}
};

#endif